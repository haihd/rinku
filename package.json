{
  "name": "rinku",
  "version": "1.0.0",
  "description": "Collect and sharing links",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node backend/server.js",
    "start:all": "concurrently \"npm run server\" \"npm start --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mendable/firecrawl-js": "^1.20.1",
    "axios": "^1.8.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
